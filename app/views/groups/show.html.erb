<div class="col-md-12">
  <div class="grooup">
    <%= link_to("Edit", edit_group_path(@group), class: "btn btn-primary pull-right") %>
  </div>
  <h2><%= @group.title %>
  <p><%= @group.description %></p>

</div>
